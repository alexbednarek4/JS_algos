function smallestDifference(arrayOne, arrayTwo) {
    // Write your code here.
      //sort arrays in ascending order
      arrayOne.sort((a, b)=>a-b);
      arrayTwo.sort((a, b)=>a-b);
      const returnArr = [];
      
      //iterate
      //if the numbers are ever equal, return them in the array
      
  }
  